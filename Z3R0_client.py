import socket
import subprocess

mi_socket = socket.socket()
mi_socket = socket.bind( ('url', 80) )
mi_socket = socket.listen(20)

while True:

#variables
 subprocess.call("dir", shell=True) 
 subprocess.call("cd", shell=True)
 subprocess.call("cls", shell=True)


#funcion

def createFile():
    data_post = {
        ''}
if __name__ == '__main__':
    createFile()



    mi_socket, addr = mi_socket.accept()
    mi_socket.send()
mi_socket.close()